{{#each entities ~}}
CREATE TABLE "{{nameSnake}}" (
{{#each properties}}
    {{#if isId }}"{{nameSnake}}" serial NOT NULL PRIMARY KEY 
    {{~else}}"{{nameSnake}}" {{type}} 
        {{~#if isRequired}}NOT NULL{{/if~}} 
        {{~#if fk}} REFERENCES "{{fk}}"("id"){{/if~}} 
    {{~/if~}}
{{~#unless @last}},{{/unless}}
{{/each}}
);

{{/each}}