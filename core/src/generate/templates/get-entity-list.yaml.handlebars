pipe:
  - id: GetList{{ name }}
    type: sql
    template: >-
      SELECT {{#each properties}}"{{ nameSnake }}"{{~#unless @last}},{{/unless}}{{/each}} 
      FROM "{{ nameSnake }}" ;    
    singleRow: false
    assign: 
      var: List{{ name }}
returnVar: List{{ name }}
transactional: false