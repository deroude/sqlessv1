pipe:
  - type: query
    statement: >-
      SELECT {{#each properties}}"{{ nameSnake }}"{{~#unless @last}},{{/unless}}{{/each}} 
      FROM "{{ nameSnake }}" ;    
    resultType: set
    assign: List{{ name }}
returnVar: List{{ name }}
transactional: false